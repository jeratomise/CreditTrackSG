node_modules/
dist/

# Environment files — never commit secrets. Covers .env, .env.production,
# .env.local, .env.*.local, etc. Only .env.example is allowed (see below).
.env
.env.*
!.env.example

.vercel

# AI prompt history — may contain secrets entered during development
migrated_prompt_history/
CreditTrackSG-main/

# Claude Code local settings — may contain bash permission history with secrets
.claude/
